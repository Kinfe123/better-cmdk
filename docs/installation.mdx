---
title: "Installation"
description: "Install and configure better-cmdk."
order: 20
---

# Installation

Install the project dependencies first, then run the command or package script that proves better-cmdk is usable in this repository.

## Install, configure, verify
Installation has two jobs: install the package dependencies and verify the command or package script that exercises the main workflow.

## Runtime inputs
- Dependencies provide the framework, CLI, adapters, and runtime libraries used by the project.
- Library options are usually supplied by the host application when it imports and initializes the package.
- Scripts provide repeatable workflows for local development, production builds, tests, and release tasks.

## Verify setup
- Dependencies install with the package manager already used by the project.
- The primary command starts, builds, or verifies the project without needing undocumented manual steps.

## Install Dependencies
Run `bun install` from the project root.

## Review Runtime Configuration
Review the package options and runtime clients that callers pass in from their application.

No reader-facing environment contract was found in README or example environment files.

## Run the project
Use `bun run dev` as the first local command for this repository.

## Runtime coverage
No framework-specific adapter or runtime files were found.

## Repository scripts
- `build`: `turbo run build` creates the production build or compiled package artifacts.
- `dev`: `turbo run dev` starts the local development workflow.
- `typecheck`: `turbo run typecheck` runs static checks before build or release work continues.
